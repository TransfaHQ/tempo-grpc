[package]
name = "indexer"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
tokio = "1.50.0"
tokio-stream = "0.1.18"
tonic = { version = "0.14.5", features = ["tls-ring", "tls-webpki-roots"] }
tracing = "0.1.44"
tracing-subscriber = "0.3.22"
